#include <vector>
#include <string>
/*
typedef enum PseudoInstruction
{
    not_a_pseudo = 0,
    set = 1,
    mul = 2,
    div = 3;
}pseudo_instruction_t;

// 0 if is not a pseudo instruction else the code of the pseudo instruction
pseudo_instruction_t* isPseudoInstruction(std::string& line);

int ReplacePseudoInstruction(std::vector<std::string>& file_content);
*/